# ============================================================
# Global Background Dictation Assistant - Requirements
# ============================================================
# INSTALLATION ORDER:
#
# Step 1: Install PyTorch (skip if already done from app.py setup)
#   With NVIDIA GPU (CUDA 12.1):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   CPU only:
#     pip install torch torchvision torchaudio
#
# Step 2: Install all other packages:
#     pip install -r requirements.txt
#
# NOTE: keyboard library may require running as Administrator on
#       some Windows configurations for global hotkey hooks.
# ============================================================

# --- Core Transcription Engine ---
faster-whisper==1.1.1

# --- Audio Capture ---
sounddevice==0.5.1
numpy==1.26.4

# --- GUI Framework (for optional Status Window) ---
customtkinter==5.2.2

# --- HTTP client (required by faster-whisper internally) ---
requests>=2.31.0

# --- System Tray Integration ---
pystray==0.19.5

# --- Image generation for tray icon ---
Pillow>=10.0.0

# --- Hotkey Support (Global Keyboard Shortcuts) ---
# keyboard allows registering global hotkeys that work even when
# the app window is not in focus.
keyboard==0.13.5

# --- Mouse & Keyboard Automation (Voice Commands) ---
# pyautogui enables programmatic mouse movement, clicks, scrolling,
# and keyboard shortcuts — used by the voice command engine.
pyautogui>=0.9.54

# --- Clipboard Access (save, paste, restore) ---
pyperclip==1.9.0

# --- LLM Routing & Semantic UI Control ---
openai
pywinauto
python-dotenv
